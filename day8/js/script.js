var value =null;
var type =null;


/*value='123' //we assign '123' into variable value
type = typeof value;  // we get the type of variable 
console.log(type);

value='123 Times Square' //we assign '123' into variable value
type = typeof value;  // we get the type of variable 
console.log(type);

value=123 //we assign '123' into variable value
type = typeof value;  // we get the type of variable 
console.log(type);

value=true //we assign '123' into variable value
type = typeof value;  // we get the type of variable 
console.log(type);

value='true' //we assign '123' into variable value
type = typeof value;  // we get the type of variable 
console.log(type);
/*123
'123 Times Square'
12.3
true
'true'
[1, 2, 3]
{ foo: 'bar' }
document

console.log(type);*/

var message_element = document.getElementById('message');
console.log(message_element);
console.log(typeof message_element);